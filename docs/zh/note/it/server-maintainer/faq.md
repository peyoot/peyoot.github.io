# 更换或重装备份服务器时无法备份
为了保证无人值所的备份，最好是在发起备份的主机上手动ssh连接远程服务器一次，因为首次连接需要添加目标服务器到know_hosts列表，需要输入yes才能继续。同样地，如果服务器更换，相关的密钥也变更，需要清除旧的记录，用ssh连接就可以发现问题并解决。
注意，你可能还需要用sudo -H tmux来以root的身份连接，它本地用的是/root/.ssh下的文件。